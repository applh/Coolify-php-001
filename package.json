{
  "name": "php-coolify-starter",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "mkdir -p dist && cp index.html dist/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.2.3"
  }
}
