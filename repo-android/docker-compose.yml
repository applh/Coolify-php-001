version: '3.8'
services:
  android-builder:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "3006:3000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: always
